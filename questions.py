"""
Python Assessment - First Year Students
======================================

Complete the following 10 functions. Each function has a docstring explaining
what it should do. Make sure to handle edge cases appropriately.

You will also need to write 2-3 unit tests for each function in student_tests.py
"""


def calculate_area(length, width):
    """
    Calculate the area of a rectangle.
    
    Args:
        length (float): The length of the rectangle
        width (float): The width of the rectangle
    
    Returns:
        float: The area of the rectangle
    """
    pass


def reverse_string(text):
    """
    Reverse a string.
    
    Args:
        text (str): The string to reverse
    
    Returns:
        str: The reversed string
    """
    pass


def is_even_or_odd(number):
    """
    Determine if a number is even or odd.
    
    Args:
        number (int): The number to check
    
    Returns:
        str: "even" if the number is even, "odd" if the number is odd
    """
    pass


def sum_of_squares(n):
    """
    Calculate the sum of squares from 1 to n.
    
    Args:
        n (int): The upper limit (inclusive)
    
    Returns:
        int: The sum of squares from 1 to n
    """
    pass


def countdown_to_zero(n):
    """
    Count down from n to 0 using a while loop.
    
    Args:
        n (int): The starting number (should be positive)
    
    Returns:
        list: A list containing the countdown sequence [n, n-1, ..., 1, 0]
    """
    pass


def find_maximum(numbers):
    """
    Find the maximum value in a list of numbers.
    
    Args:
        numbers (list): A list of numbers
    
    Returns:
        float: The maximum value in the list
    """
    pass


def count_word_frequency(text):
    """
    Count the frequency of each word in a text.
    
    Args:
        text (str): The text to analyze
    
    Returns:
        dict: A dictionary with words as keys and their frequencies as values
    """
    pass


def calculate_bmi(weight, height):
    """
    Calculate Body Mass Index (BMI). Use the formula bmi = weight/ (height**2)
    
    Args:
        weight (float): Weight in kilograms
        height (float): Height in meters
    
    Returns:
        float: The BMI value
    """
    pass


def filter_even_numbers(numbers):
    """
    Filter out even numbers from a list, keeping only odd numbers.
    
    Args:
        numbers (list): A list of integers
    
    Returns:
        list: A new list containing only the odd numbers
    """
    pass


def grade_calculator(scores):
    """
    Calculate the average grade and return a letter grade.
    
    Args:
        scores (list): A list of numerical scores
    
    Returns:
        str: The letter grade based on average score:
             'A' for 90+, 'B' for 80-89, 'C' for 70-79, 'D' for 60-69, 'F' for below 60
    """
    pass
